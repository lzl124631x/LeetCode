#include <vector>
#include <algorithm>
using namespace std;

#define SWAP(a, b) do{int tmp = a; a = b; b = tmp;}while(0)
class Solution {
public:
    void nextPermutation(vector<int> &num) {
		        
    }
};